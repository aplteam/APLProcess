 R←Test_Misc_801(stopFlag batchFlag);⎕IO;⎕ML;⎕TRAP;parms;qdmx
⍝ Try to instantiate APLProcess with ⎕NEW (bound to fail)
 ⎕IO←1 ⋄ ⎕ML←1
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 parms←##.APLProcess.CreateParms
 parms.WorkspaceName←FilesAndDirs.EnforceSlash ##.CiderConfig.HOME,'\Tests\Test.dws'

 :Trap 11
     {}⎕NEW ##.APLProcess(,⊂parms)
     →T.FailsIf 1
 :Else
     qdmx←⎕DMX
     →T.PassesIf'Please use the method APLProcess.New rather than ⎕NEW'≡qdmx.EM
 :EndTrap

 R←T._OK
⍝Done
